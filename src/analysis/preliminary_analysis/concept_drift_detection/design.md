# concept_drift_detection 設計書（Phase1：事前分析 / 集合Transformer × 転移学習版）

## 0. 位置づけ・要旨

レビュー優先順位の**「判断基準が時間で変化する（コンセプトドリフト）」かを検出する事前分析（Phase1）**の設計。

- **アプローチ**：同一計測点の Change を**集合として文脈込みで学習する Transformer**（自己注意）＋
  **転移学習（事前学習エンコーダ ＋ linear probing）**。
- 従来手法（LightGBM/RF・pointwise）は本ディレクトリから削除済み（git commit `bcea00a` に保全。将来ベースラインとして復元可能）。**今回の実行対象は集合Transformer のみ**。
- **本設計は Phase1（ドリフトの検出）に限定**。Phase2（適応）や各種拡張は §12 に「将来の方針」として明記し、実装は**拡張しやすい形**に留める。

---

## 1. 目的変数（§target）

- 各 `(Change, 計測点 T)` について、**「T から Δ 以内に人間レビューが付くか」の2値**（正例=1 / 負例=0）。
- **Δ = 1 日**（`REVIEW_HORIZON_DAYS = 1`）。
- 損失は **pointwise binary cross-entropy**（集合の文脈を見た上で、各 Change に 0/1 を予測）。
- ラベル定義：`[T, T+Δ]` に人間レビューあり→1／無く窓を完全観測できる（`T+Δ ≤ 観測末尾`）→0／窓が観測末尾超で未確定→None（除外）。

---

## 2. 対象・データ

- **プロジェクト**：nova。**対象バージョン：26.0.0 – 30.0.0（5バージョン）**。
- **分析期間（開発サイクル）**：**2022-03-30 〜 2024-10-02**。
  - `get_release_cycle` の定義：cycle_end = 当該リリース日、cycle_start = 直前リリース日（＝そのバージョンの開発サイクル）。
- **収集データ範囲**：Change `created` 2011-09-22 〜 2024-12-23（`updated` 〜 2024-12-30）。
  - 2025年分までは収集完了しているが、**26.0.0–30.0.0 の分析には不要**（各サイクルが 2024-10 までに収まるため）。

---

## 3. 計測点・アクティブ集合・「集合」の単位

- **計測点 T**：毎日 0 時の定点グリッド（`MEASUREMENT_STEP_DAYS = 1`）。
- **T のアクティブ集合**：`T − LOOKBACK ≤ created ≤ T < decision_time`（`LOOKBACK_DAYS = 365`）を満たす Open な Change。
- **この「T のアクティブ集合」が、集合モデルの 1 入力単位**（1 スナップショット＝1 例）。
- 集合内の各 Change にラベル（Δ以内レビュー 0/1）を付ける。

---

## 4. ビン（距離 `d` × 位置 `p`）

- **各リリースを、そのリリース自身の期間で 6 分割**（`BIN_COUNT = 6`）。**リリース境界に整合**、境界は **0 時に揃える**（`BIN_DAY_ALIGNED`）。
- **`d = 6` がちょうど 1 リリース**（同フェーズ）に対応。
  - 距離 `d`：滞留（**リリースフェーズ単位**。時間一定ではない点に注意）。
  - 位置 `p`：当該リリース内の位置。
- **ドリフトの読み方**：**距離 `d` 固定で位置 `p` を横断**したときの精度変化を見る。
- セル `(d, p)`：学習ビン `i = p − d`、評価ビン `p`。旧手法同様に**学習ビン単位でモデルを使い回す**（同一学習ビンを共有するセルで再学習しない）。

> 注：ビン境界が0時に揃うため、Δ=1日のとき学習末尾マージンによる除外は原則不要（境界＝計測点）。

---

## 5. モデル：集合 Transformer

### 5.1 入力・トークン化
- 入力＝T のアクティブ集合。**各 Change ＝ 15 次元のメトリクス**（変更行数・滞留期間・レビュアー数・開発者/プロジェクト系指標など。`src/features`）。
- **各 Change を線形射影して 1 トークン**にする（＝「1 Change = 1 トークン」）。
- **特徴ごとのトークン化（FT-Transformer 方式の特徴間相互作用）は今回は行わず、拡張余地として残す**（§12）。

### 5.2 エンコーダ（自己注意）
- **自己注意（Transformer エンコーダ）**、**位置エンコーディングを付けない**＝**置換不変・可変長**（集合として扱う）。
- 各 Change が同一 T の他 Change を参照 → **文脈込みの per-Change 表現**を出力。
- 小規模（既定：`d_model` 64–128、層 2、ヘッド 4、dropout 0.1）。**すべて constants で変更可能**。

### 5.3 ヘッド（linear probing）
- 既定：**linear probe（線形1層 → シグモイド）**。
- **小さな MLP ヘッド（非線形 probe）に切替可能**（constants で層数指定）。linear が弱すぎる場合のフォールバック。

---

## 6. 学習手順：転移学習（事前学習 ＋ linear probing）

### 6.1 事前学習（教師あり・共有エンコーダ）
- **プロジェクト開発初期 〜 25.0.0（＝26.0.0 の直前 2022-03-30 まで）の全過去データ**で、
  **「集合エンコーダ ＋ 仮ヘッド」を『Δ以内レビュー(0/1)』予測で学習**。
- 学習後、**仮ヘッドは破棄、エンコーダを凍結**。**共有エンコーダ**として全セルで使い回す。
- 方式は**教師あり**（constants で自己教師ありへ切替可能な形にする）。

### 6.2 linear probing（per-bin）
- 各**学習ビン `i`** について、**凍結エンコーダの埋め込み**の上に**ヘッド（linear probe）を学習** → **評価ビン `p`** で評価。
- **学習 Change 数は固定しない（フル集合）**。集合を間引かない（間引くと文脈が歪む・train/eval ミスマッチ）。

### 6.3 リーク防止
- **エンコーダは分析対象（26.0.0–30.0.0）のビンを学習に含めない**（締め切り 2022-03-30 より前のみで事前学習）。
  - 分析対象のビンは「どこかのセルの評価ビン」になりうるため、共有エンコーダに含めると評価リークになる。
  - 25.0.0 以前（学習ビンにしか使われない範囲）は含めてよい（評価ビンにならない）。
- **凍結エンコーダを分析対象データに“推論で”当てるのはリークではない**（重みは学習していない）。

### 6.4 埋め込みキャッシュ
- 凍結エンコーダで **(Change, T集合) 単位に埋め込みを 1 回計算してキャッシュ**（集合ごとに 1 回順伝播）。
- 以降の linear probe・反復・評価は**キャッシュ済み埋め込みの上で高速に**行う。

### 6.5 反復（N_REPEATS ＝ 事前学習の反復）
- **`N_REPEATS = 10`** は「**別 seed（`RANDOM_SEED + k`）で事前学習を 10 回 → 10 個のエンコーダ**」を意味する。
  - linear probe は凍結表現の上で**ほぼ決定的**なため、probe だけの反復では真のばらつきが出ない。
  - **ばらつきの主因は事前学習（NN 学習：初期値・データ順・非凸）** → **事前学習を反復してこそ意味のある不確かさ**が得られる。
- 各エンコーダで d×p の probe を回し、**10 個の行列を集約（中央値＝セル値、IQR＝エンコーダ間ばらつき）**。
- **実装初期に「1 回の事前学習時間」を実測**し、10 回が現実的か確認（規模的には軽い見込み。重ければ回数を下げる）。

### 6.6 fine-tuning を採用しない理由と拡張性

本設計は **linear probing を採用し、fine-tuning は採らない**。理由は以下の4点：

1. **少データ**：各学習ビンは ~30 集合と少ない。fine-tuning は**全層の重みを更新**するためパラメータが多く、**この少データでは過学習**しやすい。linear probe は**最後の分類層だけ**なので少パラメータで、少データに適合できる。
2. **ドリフト測定がクリーン**：linear probing は**共有エンコーダを凍結**するので、精度変化を「**ヘッド（判断ルール）の転移**」に帰属できる。fine-tuning は**ビンごとにエンコーダも変わる**ため、「**表現の変化**」と「**判断基準の変化**」が交絡し、ドリフトの読み取りが濁る。
3. **コスト**：fine-tuning は各セル×反復で**全層を逆伝播** → 重い。linear probe は**キャッシュ済み埋め込みの上**で学習でき安い（§6.4）。
4. **分布外での頑健性**：fine-tuning は事前学習で得た表現を歪め、**分布外（out-of-distribution）で linear probing に劣ることがある**（Kumar et al., ICLR 2022「Fine-Tuning can Distort Pretrained Features…」）。ドリフト（位置で分布が変わる）では **linear probing の方が頑健**になりうる。

**拡張性（fine-tuning / PEFT へ切り替え可能に設計）**：
- 「fine-tuning や PEFT（LoRA・adapter 等）が有効そうなら切り替えられる」よう、**チューニング方式を constants で選択可能**にする（例：`TUNING = "linear_probe" | "fine_tune" | "peft"`）。
  - `linear_probe`：エンコーダ凍結、ヘッドのみ学習（既定）。
  - `fine_tune`：エンコーダを解凍して per-bin 学習。
  - `peft`：少数の追加パラメータ（LoRA/adapter 等）のみ更新。
- **段階的解凍**（linear_probe → 上位層のみ → 全層）も選べる形にしておけば、**Kumar 2022 の LP→FT（先に線形プローブ→その後微調整）**も試せる。
- ＝**既定は linear probing、必要に応じて fine-tuning/PEFT へ拡張**、という位置づけ。

---

## 7. データ量の扱い（3種）

| データ | 扱い |
|---|---|
| ① ヘッド（linear probe）学習：学習ビン | **固定しない ＋ サイズ記録・チェック ＋ MIN 床** |
| ② 評価：位置ビン | **固定しない ＋ チェック ＋ MIN 床** |
| ③ 事前学習：エンコーダ | **プロジェクト初期〜2022-03-30 の全過去データ（1回／反復時は seed 違い）** |

- **サイズは固定しない**（フル集合）。旧手法の「学習 Change 500 固定」は**廃止**。
- 公平性は**「サイズの記録＋サニティチェック（精度差が量差で説明できないか）」**で担保。
- **MIN 床**：供給が下限未満のセルは NaN（信頼性）。`MIN_TRAIN` / `MIN_EVAL`（暫定 30、constants）。

---

## 8. 評価

### 8.1 指標
- **主指標：AUC**（しきい値フリー・不均衡に頑健。Δ=1 は正例が稀なため）。必要に応じ **AP（PR曲線下面積）**も。
- **補助：precision / recall / f1**。しきい値は**当面 0.5**（constants `CLASSIFY_THRESHOLD` で変更可、後で全セル共通の調整済みしきい値に置換可能）。**per-cell のしきい値調整はしない**（比較の交絡を避ける）。
- プールして算出（評価レコード全体）。
- **距離×時期行列のプロットは AUC と f1 の両方を必ず出力**（＋ precision / recall）。AUC は主判断、f1 は運用的な見え方の補助として並べて確認する（§10）。

### 8.2 ドリフト検定
- **既存の並べ替え検定（permutation test）＋変化点（changepoint）**で、位置横断の精度変化が有意か判定（予測に対して実施、再学習しない）。
- **リリース横断の本数集計**（N 本中 k 本でドリフトあり）。
- ドリフトの**有無は検定で判断**（反復 IQR ではない）。

### 8.3 「汎用ヘッド」vs「per-bin probe」比較
- **事前学習の仮ヘッド（＝広い平均的な読み取り。以下「汎用ヘッド」）を各位置 p で評価**（推論のみ・安価）し、
  **per-bin の probe の位置別精度と並べて比較** → **「セルごとに特化させる価値があるか」**を見る。
- 両者とも同じ評価ビンで評価（公平）。汎用ヘッドの学習締め切りは分析対象より前（リークなし）。
- **汎用ヘッドの結果も保存し、距離×時期行列のプロットを作成する**（per-bin probe と同じ形式で並べて比較できるように）。
  - **注意：汎用ヘッドは距離 `d` に依存しない**（学習ビンを持たない単一の固定モデルのため、値は位置 `p` のみで決まる）。
  - したがって行列表示では **各行 `d` に同じ値を入れる（列 `p` ごとに一定＝横帯）**形で d×p 図に落とす（probe 行列と同一の軸・形式で視覚比較するため）。
  - **AUC と f1 の両方**を出力（§8.1）。
- **差分行列（probe − 汎用ヘッド）を必ず作成する**（値 ＋ プロット png、AUC・f1 の両方）。
  - probe 行列は `d` に依存、汎用ヘッドは列 `p` ごとに一定なので、差分 `probe(d,p) − 汎用(p)` は**正常な d×p 行列**になる。
  - **正＝特化が効く（probe が汎用より良い）、負＝特化が悪化（少データで probe が汎用に負ける）**。→ 「どの `(d, p)` で特化が効くか」を直接可視化。

### 8.4 near/far（表現の古さ）への留意
- 共有エンコーダは 2022-03 で凍結のため、**後のバージョンほど表現が“古い”**（near/far）。精度低下が「基準の変化」でなく「表現の古さ」による可能性がある、という**留意点**。
- ただし**リリース内の位置ドリフト（距離 d 固定で位置 p 横断）は staleness にほぼ影響されない**（古さはリリース内でほぼ一定）ので、主な読み取りは比較的安全。
- **検定などの自動チェックは今回は実装しない**。結果を見て**人手で判断**する（必要になれば §8.3 の「汎用ヘッド vs probe」比較などで後から確認可能）。

---

## 9. 反復・集約・計算環境

- 反復＝**事前学習の反復（`N_REPEATS = 10` エンコーダ）**。集約：**中央値（セル値）＋ IQR（不確かさ）**。
- **torch ＋ MPS（Apple M2）**。CUDA は無し。主コストは**事前学習 ＋ 埋め込みキャッシュ**、probe は安い。

---

## 10. 出力

- **d×p 行列（値 ＋ プロット png）を指標ごとに出力**（セル値＝中央値、IQR）。**プロットは AUC と f1 の両方を必ず出力**（＋ precision / recall）。
- **汎用ヘッドの d×p 行列（値 ＋ プロット png）も保存・出力**（§8.3。距離 `d` に非依存なので列 `p` ごとに一定＝横帯）。AUC・f1 の両方。
- **差分行列（probe − 汎用ヘッド）を必ず出力**（値 ＋ プロット png、AUC・f1）。正＝特化が効く／負＝特化が悪化（§8.3）。
- **drift_test.json**（p値・有意判定・変化点）、**drift_count**（リリース横断の本数集計）。
- **predictions**（評価レコードの `y_true(0/1)` / `y_pred(正例確率)` を保存）→ 後から指標・検定を再計算可能。
- 出力の構造は旧 `concept_drift_detection` を踏襲（`<project>/<model>/<version>/<metric>/` ＋ summary）。

---

## 11. パラメータ（constants に集約）

主なもの（すべて constants で変更可能に）：
- 目的変数：`REVIEW_HORIZON_DAYS = 1`、`CLASSIFY_THRESHOLD = 0.5`。
- ビン：`BIN_COUNT = 6`、`BINNING`、`BIN_DAY_ALIGNED`、per-release 分割。
- 計測点：`MEASUREMENT_STEP_DAYS = 1`、`LOOKBACK_DAYS = 365`。
- モデル：エンコーダ規模（`d_model`・層数・ヘッド数・dropout）、**ヘッド種別（linear / small-MLP）**、事前学習方式（教師あり / 自己教師あり）。
- 学習：事前学習の締め切り（2022-03-30）、`N_REPEATS = 10`（＝事前学習反復）、`MIN_TRAIN` / `MIN_EVAL`、`RANDOM_SEED`。
- 指標：`ENABLED_METRICS`（AUC 主 ＋ precision/recall/f1）、検定 `PERMUTATION_N`・`SIGNIFICANCE`。

---

## 12. スコープ外（今回）＋ 将来の拡張性（拡張しやすい形で設計）

本設計は Phase1 に限定するが、以下を**後から足せるモジュール構造**にしておく：

1. **Phase2（適応）**：内部表現で“近い”Change を選んで（類似度で）再チューニング → 現在の基準に特化した予測。ドリフト確認後に着手。
2. **目的変数の拡張**：`reviewed_within_delta` 以外に **merge / abandoned など評価軸を追加**できる形に（ラベル計算を差し替え可能に）。
3. **ベースライン比較**：**従来手法（LightGBM/RF・pointwise。git `bcea00a`）と比較**する予定 → 同じ d×p・指標で比較しやすい形に。
4. **入力（特徴量）の変更/拡張**：**タイトル/アブストラクト（テキスト）・コード（CodeBERT 等）・著者**などを追加可能に。
   - 構造：**「トークン化（型ごと：テキスト/コードは既製モデル、数値/著者は埋め込み）→ 融合（トークン間の自己注意）→ 集合（Change 間の自己注意）」の 3 段**。
   - **特徴ごとのトークン化（FT-Transformer 方式）**もこの融合段で追加可能。
5. **特徴帰属（なぜ効いたか）**：**Integrated Gradients / Permutation Importance / attention 重み**で「どの特徴・どの他 Change が効いたか」を**後から算出**できる形に（Phase1 では検出に集中）。
6. **タスク形式の拡張（分類 → 順位・回帰）**：現在は **pointwise の2値分類**（Δ以内にレビューされるか否か）のみで評価しているが、将来は以下も行いたい：
   - **順位（ranking / learning-to-rank）**：T 内で「**どの Change を先にレビューすべきか**」の**並び**を学ぶ（listwise 損失。集合Transformer と相性が良い）。指標は NDCG / MAP など。
   - **回帰（regression）**：2値でなく**連続量**（例：次のレビューまでの時間 `time_to_next_review`）を予測。
   - いずれも**集合Transformer（エンコーダ）は共通で使え**、**ヘッド・損失・評価指標を差し替えるだけ**で対応できる形にしておく（`TASK = "classification" | "ranking" | "regression"` のように切替可能に）。

---

## 13. ディレクトリ・コード構成（予定）

`src/analysis/preliminary_analysis/concept_drift_detection/` に新規作成：
- `utils/constants.py`：全パラメータ。
- `labeling/`：Δ以内2値ラベル。
- `dataset/`：レコード生成（(Change,T)）・集合構築・ビン割当（per-release 6分割・0時揃え）。
- `model/`：集合Transformer（エンコーダ）・ヘッド（linear/small-MLP）・事前学習（教師あり）・埋め込みキャッシュ。
- `evaluation/`：指標（AUC 主 ＋ precision/recall/f1）・d×p 行列（学習ビン使い回し）・ドリフト検定・汎用ヘッド vs probe 比較。
- `io/`：行列/検定/本数集計/predictions の入出力。
- `main.py`：run / analyze / recompute / replot。
- torch（MPS）を実装時に導入。

旧 LightGBM/RF は git（`bcea00a`）に保全。将来ベースラインとして復元可能。

---

## 14. 実装方針（可読性・初学者向け）

**初学者でも理解しやすいコードを最優先する**（研究の引き継ぎ・再現性のため）。具体的には：

- **明確な命名**：略語を避け、意図が伝わる変数・関数名にする（`x`, `tmp` などは避ける）。
- **1 モジュール 1 責務**：ラベル計算・集合構築・ビン割当・モデル・学習・評価・入出力をファイルで分離（§13）。1 関数は 1 つのことだけ行う。
- **docstring と要所コメント**：各モジュール・関数の冒頭に「何をするか・入出力・前提」を日本語で明記。特に**非自明な箇所（自己注意で集合を扱う理由・位置エンコを付けない意味・事前学習→凍結→linear probing の流れ・リーク防止・埋め込みキャッシュ）**には根拠コメントを付ける。
- **設計書との対応**：コード内コメントで対応する設計書の節（例「§6.3 リーク防止」）を参照し、コードと設計の対応を追える形にする。
- **読みやすさ優先**：技巧的な 1 行より、素直で追いやすい実装を選ぶ（トリッキーな内包表記・過度な抽象化を避ける）。
- **パラメータは constants に集約**（§11）：マジックナンバーを埋め込まず、意味と既定値をコメント付きで 1 箇所に。
- **段階的に読める構成**：`main.py` から「データ生成 → ビン → 事前学習 → linear probing → 評価」の流れが**上から順に追える**ようにする。
